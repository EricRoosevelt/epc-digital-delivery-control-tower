"""Checker implementations.

A checker answers "how is this requirement evaluated?". ``IdsChecker`` is the
built-in, backed by IfcTester; IDS compilation is an internal detail of it
rather than a stage the whole pipeline passes through.
"""
